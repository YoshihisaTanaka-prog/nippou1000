<%= form_with(model: what_to_do) do |form| %>
  <% if what_to_do.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(what_to_do.errors.count, "error") %> prohibited this what_to_do from being saved:</h2>

      <ul>
        <% what_to_do.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :time, style: "display: block" %>
    <%= form.datetime_field :time %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
