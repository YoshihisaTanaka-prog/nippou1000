<%= form_with(model: learned_list) do |form| %>
  <% if learned_list.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(learned_list.errors.count, "error") %> prohibited this learned_list from being saved:</h2>

      <ul>
        <% learned_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :text, style: "display: block" %>
    <%= form.text_area :text %>
  </div>

  <div>
    <%= form.label :is_learned, style: "display: block" %>
    <%= form.check_box :is_learned %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
